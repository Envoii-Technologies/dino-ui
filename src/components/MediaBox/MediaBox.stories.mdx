import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import { MediaBox } from './MediaBox';

export const Template = (args) => (
	<MediaBox { ...args } />
);

<Meta title='Components/MediaBox' component={ MediaBox } />

# MediaBox

...

## Examples

### Default

...

<Canvas>
	<Story
		name="Default"
		args={{
			image: '/images/test.png',
			size: 'md',
			onEdit: (data) => alert(data),
			onDelete: (data) => alert(data)
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

## API

<ArgsTable of={ MediaBox } />
