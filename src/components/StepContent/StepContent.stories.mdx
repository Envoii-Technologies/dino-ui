import { useState } from 'react';
import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { StepContent } from './StepContent';

export const Template = (args) => {
	const [stepInfoData, setStepInfoData] = useState({});
    return (
        <>
            <StepContent {...args} onStepChange={data => setStepInfoData(data)}/>
			<code>
            {
                JSON.stringify(stepInfoData)
            }
            </code>
        </>
    );
};

<Meta title="Components/StepContent" component={StepContent} />

# StepContent

...

## Examples

### Default

...

<Canvas>
    <Story
        name="Default"
        args={{
            onAddStep: () => alert('[NOT IMPLEMENTED]'),
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## API

<ArgsTable of={StepContent} />
