import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { ModalContentWindow } from './ModalContentWindow';
import { Button } from './../../';

export const Template = (args) => {
    const [showWindow, setShowWindow] = React.useState(false);
	// const [newNotification, setNewNotification] = React.useState();

    //     const createNotification = (data) => {
    // 		setNewNotification({ ...data, id: Date.now() + Math.floor(Math.random() * 10000) });
    // 	}

    return (
        <>
    		<Button label="Show ModalContentWindow" onClick={() => setShowWindow(true)} />
		  	<ModalContentWindow { ...args } show={showWindow} body="This is a Modal Content Window!" onCancel={() => setShowWindow(false)} onAccept={() => alert("[NOT IMPLEMENTED]")} />
        </>
    );
};

<Meta title="Components/ModalContentWindow" component={ModalContentWindow} />

# ModalContentWindow

...

## Examples

### Default

...

<Canvas>
    <Story name="Default" args={{}}>
        {Template.bind({})}
    </Story>
</Canvas>

## API

<ArgsTable of={ModalContentWindow} />
