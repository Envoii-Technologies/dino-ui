import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { OverflowMenu } from './OverflowMenu';

import { faFlag, faCalendar } from '@fortawesome/free-regular-svg-icons';
import {
    faTag,
    faQrcode,
    faAlignLeft,
    faGlobe,
} from '@fortawesome/free-solid-svg-icons';

export const Template = (args) => <OverflowMenu {...args} />;

<Meta title="Components/OverflowMenu" component={OverflowMenu} />

# OverflowMenu

...

## Examples

### Default

...

<Canvas>
    <Story
        name="Default"
        args={{
            header: "Some Content...",
            content: [
                [
                    {
                        title: 'Tags',
                        icon: faTag,
                        onClick: () => alert("Tags")
                    },
                    {
                        title: 'Klassifizierung',
                        icon: faFlag,
                        onClick: () => alert("Tags")
                    },
                    {
                        title: 'Deadline',
                        icon: faCalendar,
                        onClick: () => alert("Tags")
                    },
                ],
                [
                    {
                        title: 'Beschreibung',
                        icon: faAlignLeft,
                        onClick: () => alert("Tags")
                    },
                    {
                        title: 'QR Code',
                        icon: faQrcode,
                        onClick: () => alert("Tags"),
                        disabled: true,
                    },
                    {
                        title: 'Sprache',
                        icon: faGlobe,
                        onClick: () => alert("Tags")
                    },
                ],
            ],
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## API

<ArgsTable of={OverflowMenu} />
